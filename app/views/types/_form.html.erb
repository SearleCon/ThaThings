<%= form_for(@type) do |f| %>
  <% if @type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type.errors.count, "error") %> prohibited this type from being saved:</h2>

      <ul>
      <% @type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <center><%= image_tag("briefdescription.jpg") %></center>
    <%= f.text_field :type_name %>
  </div>

  <div class="actions">
    <%= f.submit value='Save!' %><%= image_tag("save.jpg") %>
    
  </div>
<% end %>